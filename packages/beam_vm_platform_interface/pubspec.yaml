name: beam_vm_platform_interface
description: A common platform interface for the beam_vm plugin.
version: 1.0.1
homepage: https://github.com/gsmlg-app/beam_vm
repository: https://github.com/gsmlg-app/beam_vm/tree/main/packages/beam_vm_platform_interface
issue_tracker: https://github.com/gsmlg-app/beam_vm/issues
topics:
  - erlang
  - elixir
  - beam-vm
  - embedded-runtime

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
